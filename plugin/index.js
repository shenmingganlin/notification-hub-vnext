/**
 * Notification Hub vNext plugin entry point.
 *
 * Phase 1 only establishes the package boundary. Domain behavior is added in
 * later phases after protocol and diagnostic contracts are defined.
 */
export const pluginVersion = '0.1.0-alpha.1';
export const pluginName = 'notification-hub-vnext';
