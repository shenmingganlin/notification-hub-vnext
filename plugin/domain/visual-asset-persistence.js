import { dirname, resolve } from 'node:path';
import { mkdir, readFile, rename, rm, writeFile } from 'node:fs/promises';

export const VISUAL_ASSET_SNAPSHOT_VERSION = 1;
function fail(code, message, details = {}) { return Object.assign(new Error(message), { code, details }); }
function plain(value) { return value !== null && typeof value === 'object' && !Array.isArray(value) && [Object.prototype, null].includes(Object.getPrototypeOf(value)); }
function validate(snapshot) { if (!plain(snapshot) || snapshot.version !== VISUAL_ASSET_SNAPSHOT_VERSION || !Array.isArray(snapshot.assets) || !plain(snapshot.references)) throw fail('VISUAL_ASSET_SNAPSHOT_INVALID', 'Visual asset snapshot is invalid'); return snapshot; }
export async function saveVisualAssetSnapshot(snapshot, filePath) { validate(snapshot); if (typeof filePath !== 'string' || !filePath.trim()) throw fail('VISUAL_ASSET_PATH_INVALID', 'filePath must be a non-empty string'); const temporary = `${filePath}.tmp-${process.pid}-${Date.now()}`; try { await mkdir(dirname(filePath), { recursive: true }); await writeFile(temporary, `${JSON.stringify(snapshot)}\n`, { encoding: 'utf8', flag: 'wx' }); await rename(temporary, filePath); return filePath; } catch (cause) { await rm(temporary, { force: true }).catch(() => {}); throw cause.code?.startsWith('VISUAL_ASSET_') ? cause : fail('VISUAL_ASSET_PERSIST_FAILED', 'Failed to persist visual asset snapshot', { path: filePath, cause: cause.message }); } }
export async function loadVisualAssetSnapshot(filePath) { try { return validate(JSON.parse(await readFile(resolve(filePath), 'utf8'))); } catch (cause) { if (cause.code === 'ENOENT') return null; if (cause.code?.startsWith('VISUAL_ASSET_')) throw cause; throw fail('VISUAL_ASSET_LOAD_FAILED', 'Failed to load visual asset snapshot', { path: filePath, cause: cause.message }); } }
