import { EventEmitter } from 'node:events';
import { readFile } from 'node:fs/promises';
import { replaceFileAtomically } from '../persistence/atomic-file-replace.js';
import { createSoundSettingsStoreSnapshot, SOUND_SETTINGS_STORE_VERSION } from './sound-settings-store.js';

const fail = (code, message, details = {}) => Object.assign(new Error(message), { code, details });
const encode = (snapshot) => { if (snapshot.version !== SOUND_SETTINGS_STORE_VERSION) throw fail('SOUND_SETTINGS_STORE_SNAPSHOT_VERSION_UNSUPPORTED', 'Unsupported sound settings snapshot version'); return `${JSON.stringify(snapshot)}\n`; };
const decode = (text) => { let value; try { value = JSON.parse(text); } catch { throw fail('SOUND_SETTINGS_SNAPSHOT_PARSE_FAILED', 'Sound settings snapshot contains invalid JSON'); } return createSoundSettingsStoreSnapshot(value.settings, value.revision, { updatedAt: value.updatedAt }); };

export async function saveSoundSettingsSnapshot(snapshot, filePath) { if (typeof filePath !== 'string' || !filePath.trim()) throw fail('SOUND_SETTINGS_PATH_INVALID', 'Sound settings path must be a non-empty string'); try { return await replaceFileAtomically(filePath, encode(snapshot)); } catch (cause) { if (cause.code?.startsWith('SOUND_SETTINGS_')) throw cause; throw fail('SOUND_SETTINGS_PERSIST_FAILED', 'Failed to persist sound settings snapshot', { path: filePath, cause: cause.message }); } }
export async function loadSoundSettingsSnapshot(filePath) { try { return decode(await readFile(filePath, 'utf8')); } catch (cause) { if (cause.code === 'ENOENT') return null; if (cause.code?.startsWith('SOUND_SETTINGS_')) throw fail('SOUND_SETTINGS_LOAD_FAILED', 'Failed to load sound settings snapshot', { path: filePath, cause: cause.code }); throw fail('SOUND_SETTINGS_LOAD_FAILED', 'Failed to load sound settings snapshot', { path: filePath, cause: cause.message }); } }

export class SoundSettingsPersistenceCoordinator extends EventEmitter {
  constructor({ store, filePath, debounceMs = 100, save = saveSoundSettingsSnapshot, load = loadSoundSettingsSnapshot, schedule = (fn, ms) => setTimeout(fn, ms), cancel = clearTimeout } = {}) { super(); if (!store?.on || !store?.getSnapshot || !store?.restoreSnapshot) throw fail('SOUND_SETTINGS_PERSISTENCE_INVALID', 'Sound settings persistence requires a compatible store'); if (typeof filePath !== 'string' || !filePath.trim()) throw fail('SOUND_SETTINGS_PATH_INVALID', 'Sound settings persistence filePath must be non-empty'); this.store = store; this.filePath = filePath; this.debounceMs = debounceMs; this.save = save; this.load = load; this.schedule = schedule; this.cancel = cancel; this.pendingSnapshot = null; this.timer = null; this.flushPromise = null; this.restoring = false; this.listener = null; }
  observe() { if (!this.listener) { this.listener = () => { if (!this.restoring) this.requestSave(); }; this.store.on('change', this.listener); } return () => this.dispose(); }
  dispose() { if (this.listener) this.store.off('change', this.listener); this.listener = null; if (this.timer !== null) this.cancel(this.timer); this.timer = null; }
  requestSave() { this.pendingSnapshot = createSoundSettingsStoreSnapshot(this.store.getSnapshot().settings, this.store.getSnapshot().revision); if (this.timer !== null) this.cancel(this.timer); this.timer = this.schedule(() => { this.timer = null; this.flush().catch(() => {}); }, this.debounceMs); return this.pendingSnapshot; }
  async restore() { this.restoring = true; try { const snapshot = await this.load(this.filePath); if (snapshot) this.store.restoreSnapshot(snapshot); return snapshot; } catch (cause) { const wrapped = cause.code === 'SOUND_SETTINGS_LOAD_FAILED' ? cause : fail('SOUND_SETTINGS_LOAD_FAILED', 'Failed to restore sound settings', { path: this.filePath, cause: cause.code ?? cause.message }); this.emit('diagnostic', { code: wrapped.code, message: wrapped.message, details: { path: this.filePath, ...(wrapped.details ?? {}) } }); throw wrapped; } finally { this.restoring = false; } }
  async flush() { if (this.timer !== null) { this.cancel(this.timer); this.timer = null; } if (this.flushPromise) return this.flushPromise; if (!this.pendingSnapshot) return null; this.flushPromise = (async () => { while (this.pendingSnapshot) { const snapshot = this.pendingSnapshot; this.pendingSnapshot = null; try { await this.save(snapshot, this.filePath); } catch (cause) { if (!this.pendingSnapshot) this.pendingSnapshot = snapshot; const wrapped = cause.code?.startsWith('SOUND_SETTINGS_') ? cause : fail('SOUND_SETTINGS_PERSIST_FAILED', 'Failed to persist sound settings', { path: this.filePath, cause: cause.message }); this.emit('diagnostic', { code: wrapped.code, message: wrapped.message, details: { path: this.filePath, ...(wrapped.details ?? {}) } }); throw wrapped; } } return this.filePath; })().finally(() => { this.flushPromise = null; }); return this.flushPromise; }
}
