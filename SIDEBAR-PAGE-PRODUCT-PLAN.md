# Notification Hub vNext
# 侧边栏与页面产品路线基准

> 文档用途：这是 Notification Hub vNext 后续设计和实现侧边栏、Widget、Notification Center、设置页、诊断页、声音页、视觉主题页之前必须读取的产品基准。
>
> 该文档记录 2026-08-11 已确认的产品方向。若后续需要改变其中的职责边界或阶段顺序，必须先重新讨论并更新本文档，不得在实现中悄悄偏离。

---

## 1. 总体产品方向

Notification Hub vNext 先完成稳定的 Windows 产品，再逐步扩展声音、视觉、模式和生态能力。

核心推进原则：

1. 先把侧边栏和页面稳定下来，完成页面结构、响应式布局、导航鉴权和主要交互闭环。
2. 再建立面向用户的通知卡片分类体系，例如聊天、频道、工具、错误、插件等。
3. 再为每个分类建立可独立配置的声音策略。
4. 再为每个分类建立可独立配置的视觉策略。
5. 再制作能够组合分类、声音和视觉规则的模式。
6. 最后开放公共通知 API，让外部插件声明自己的分类、声音和视觉需求。
7. 最后进行长时间运行、性能、回滚和正式发布验证。
8. 当前不考虑跨平台 Runtime 阶段。

质量优先级固定为：

```text
稳定性和可恢复性
→ 可维护性
→ 性能和流畅度
→ 视觉质量和可定制性
→ 功能数量
```

当前交付版本：

```text
0.1.0-alpha.11
```

Native Runtime 交互稳定基线来自 alpha.8。

alpha.8 已经通过真实 Hana 验收：

- 连续创建卡片时不再在左上角闪现。
- 鼠标按下、松开只会关闭一张卡片。
- 多卡片关闭后剩余卡片能够正确重排。

后续任何功能都必须保护这条稳定基线。

---

## 2. 确认后的阶段顺序

采用以下产品推进顺序：

```text
阶段 0：冻结稳定基线 + 侧边栏问题测量
    ↓
阶段 1：侧边栏与页面稳定
    ↓
阶段 2：通知卡片分类体系
    ↓
阶段 3：分类声音策略
    ↓
阶段 4：分类视觉策略
    ↓
阶段 5：模式制作
    ↓
阶段 6：公共通知 API
    ↓
阶段 7：稳定化与正式发布
```

Runtime 生命周期、Shelf 多卡片、Notification Center、侧边栏响应式布局和页面导航属于阶段 1 的稳定性基础；它们完成后才进入分类、声音和视觉能力。

阶段 8 暂不考虑：

```text
阶段 8：跨平台 Runtime
```

### 2.1 阶段 0：冻结稳定基线 + 侧边栏问题测量

目标：不改变当前功能行为，先确认侧边栏实际宽度问题和信息截断位置。

工作内容：

- 保留 alpha.8 的 Runtime 回归测试。
- 记录 alpha.8 为当前稳定基线。
- 在真实 Hana 中测量 Widget 宿主宽度和实际内容宽度。
- 区分“Widget CSS 太窄”和“Hana 宿主容器太窄”。
- 记录标题、摘要、状态、按钮、音量控件和主题名称的截断位置。
- 建立响应式布局基线。
- 不提前加入完整声音、视觉或模式设置。

完成条件：

- 知道侧边栏的实际可用宽度。
- 知道内容被截断的真实原因。
- 确定最小可用宽度和舒适宽度目标。
- 不破坏 alpha.8 的桌面交互行为。

### 2.2 阶段 1A：Runtime 生命周期收口

目标：让 Runtime 的启动、连接、重连、停止、异常退出和恢复形成稳定闭环。

重点验证：

```text
Hana 启动
→ Runtime 启动
→ Named Pipe 连接
→ 创建卡片
→ 关闭卡片
→ Runtime 正常停止
→ 再次启动并恢复状态
```

必须覆盖：

- Runtime 启动状态。
- Named Pipe 连接状态。
- Runtime 重连状态。
- 主动停止不误报崩溃。
- 异常退出产生结构化诊断。
- Runtime 重启后 Node 插件重新连接。
- 不残留旧 Runtime 进程。
- 侧边栏只显示健康摘要，详细诊断交给页面。

### 2.3 阶段 1B：Shelf 多卡片与布局稳定性

目标：把已有 Stack/Shelf 行为做成可以长期使用的稳定能力。

必须验证：

- 多卡片连续创建。
- 创建第二张及后续卡片不闪现。
- 关闭第一张卡片。
- 关闭中间卡片。
- 关闭最后一张卡片。
- 关闭后剩余卡片重排。
- 卡片自动过期。
- 新卡片加入时已有卡片不异常跳动。
- SceneState 与真实窗口位置一致。
- Hana 重启后不恢复幽灵卡片。
- 多显示器和 DPI fixture 的边界行为。
- 布局失败时回退到稳定布局并产生诊断。

侧边栏只显示：

```text
当前卡片数量
当前布局摘要
Runtime 是否正常
```

Shelf 的详细参数和调试控制不属于侧边栏的长期产品职责。

### 2.4 阶段 1C：Notification Center、侧边栏与页面稳定闭环

目标：让页面承担完整的通知历史管理，Widget/侧边栏只承担轻量入口。

Notification Center 逐步完善：

- 全部通知。
- 未读通知。
- 重要通知。
- 错误通知。
- 聊天通知。
- 工具通知。
- 系统通知。
- 频道通知。
- 标题和正文搜索。
- 按时间和重要性排序。
- 单条详情。
- 批量标记已读。
- 批量删除。
- 清空历史。
- 空状态、加载状态和 API 失败状态。
- 长正文、长元数据和多通知性能。
- 详情展开不自动改变已读状态。

页面负责完整功能，侧边栏只显示通知简要和快速入口。

### 2.5 阶段 2：通知卡片分类体系

> 当前阶段 1 的侧边栏与页面稳定闭环完成后，才进入本阶段。

目标：建立用户能理解、策略层能稳定引用、外部插件未来也能声明的通知分类体系。

首批用户可见分类包括：

- 聊天：助手回复、普通对话完成等桌面聊天通知。
- 频道：Telegram、飞书、QQ、微信等外部通信渠道通知。
- 工具：工具调用、工具结果和工具执行状态通知。
- 错误：工具错误、提供商错误、超时、限流和其他失败通知。
- 插件：由外部 HanaAgent 插件生产或提交的通知。

分类不能简单覆盖或改写 `type`、`source`、`channel`、`producer` 等领域字段。它是面向展示和策略的分类投影；一条通知可以同时具有基础分类和错误/插件等策略标签，例如“频道中的错误通知”或“插件产生的工具错误”。

完成条件：

- 分类规则对真实通知字段有明确来源。
- 分类结果不修改原始通知。
- 分类结果可被 Notification Center 查询和展示。
- 分类结果可以被声音和视觉策略共同使用。
- 没有真实外部插件数据时，不伪造插件通知，只保留协议和规则边界。

### 2.6 阶段 3：分类声音策略

目标：让每个通知分类都可以独立配置声音，同时保留全局默认和稳定回退。

声音策略必须建立在阶段 2 的分类结果上，至少支持：

- 聊天通知的独立声音。
- 频道通知的独立声音。
- 工具通知的独立声音。
- 错误通知的独立声音。
- 插件通知的独立声音。
- 全局默认声音和分类未配置时的回退。
- 分类规则与重要性、重复通知策略的组合。

声音基础：

- 音频适配器。
- 全局启用 / 禁用。
- 音量。
- 分类声音规则解析器。
- 声音预览。
- 播放失败回退。
- 设置保存、应用和失败状态。

### 2.7 阶段 4：分类视觉策略

目标：让每个通知分类都可以独立配置视觉表现，并与声音策略使用同一套分类匹配结果。

视觉策略至少支持：

- 每个分类独立的卡片主题或视觉预设。
- 强调色、动画强度和粒子能力的分类覆盖。
- 分类未配置时回退到全局视觉默认。
- 视觉预览与可读性检查。
- 软件简化回退。
- 侧边栏、Notification Center、设置页和诊断页共享主题变量。

主题系统至少统一以下 Token：

```text
背景
面板
卡片
边框
主文字
次文字
强调色
成功色
警告色
错误色
按钮背景
按钮文字
焦点轮廓
阴影
```

此阶段先做少量高质量默认主题和视觉预设，建立注册、分类覆盖、预览、可读性和回退机制，不用大量选项掩盖底层质量问题。

### 2.8 阶段 5：模式制作

目标：在声音、视觉和主题基础成熟后，让用户在页面中制作和管理模式。

模式制作必须放在页面，不放进侧边栏首层。

模式可以组合：

- 通知显示规则。
- 重要性阈值。
- 显示位置。
- 显示时长。
- 布局。
- 动画。
- 粒子。
- 声音。
- 是否保存历史。
- 是否允许桌面显示。

模式编辑需要有：

- 当前值。
- 来源解释。
- 预览。
- 保存状态。
- 应用状态。
- 应用失败回退。
- 恢复默认。

侧边栏只允许提供未来的轻量模式快速切换入口；模式创建、继承、覆盖和详细编辑全部由页面负责。

### 2.9 阶段 6：公共通知 API

目标：在领域模型、声音、视觉、模式和诊断边界稳定后，再开放给其他 HanaAgent 插件。

公共 API 必须先确定：

- 版本。
- schema。
- 插件身份。
- 权限范围。
- 能力发现。
- 错误码。
- 诊断字段。
- 版本迁移。
- 示例调用。

公共 API 可以包含：

```text
send
list
get
markRead
markUnread
dismiss
getCapabilities
```

公共 API 的详细管理不进入侧边栏。侧边栏最多显示公共服务状态摘要，完整说明和权限管理由页面承担。

### 2.10 阶段 7：稳定化与正式发布

必须覆盖：

- 长时间运行测试。
- 多显示器测试。
- 混合 DPI 测试。
- Runtime 重启测试。
- 资源泄漏检查。
- 性能基准。
- 声音失败回退。
- 视觉主题回归。
- 页面与侧边栏状态同步。
- 设置保存 / 应用 / 失败状态。
- 发布打包。
- 回滚包。
- 变更日志。
- 用户迁移文档。

---

## 3. 侧边栏的产品定位

侧边栏是轻量化的快速工具，不是完整设置中心。

一句话定义：

> 侧边栏是驾驶仪表盘，页面是控制台。

侧边栏负责：

1. 通知简要。
2. Runtime 状态摘要。
3. 简单的音量控制。
4. 快速的视觉设置。

侧边栏不负责完整配置、复杂筛选、模式制作和公共 API 管理。

### 3.1 侧边栏负责的内容

#### 通知简要

可以显示：

- 未读数量。
- 最近 3 条通知。
- 标题。
- 简短摘要。
- 重要性。
- 时间。
- 快速打开详情。
- 打开 Notification Center。

不显示：

- 完整通知历史。
- 复杂搜索和筛选。
- 批量管理。
- 长正文全文。
- 高级分类条件。

#### Runtime 状态

只显示健康摘要：

```text
● 正常运行
Pipe 已连接
当前卡片：2
```

异常时显示：

```text
● 需要注意
连接已断开
[查看诊断]
```

详细信息必须放页面：

- 错误码。
- traceId。
- 生命周期事件。
- 重连记录。
- SceneState。
- 进程信息。
- 恢复结果。
- 诊断导出。

#### 快速声音设置

允许：

- 全局静音开关。
- 简单音量滑块。
- 可选的快速试听。
- 显示“已应用 / 应用失败”短状态。

不允许在侧边栏配置：

- 按通知类型配置声音。
- 按重要性配置声音。
- 重复通知策略。
- 工作模式静音规则。
- 声音文件管理。
- 自定义音频路径。
- 播放失败回退细节。

#### 快速视觉设置

允许：

- 快速切换已注册主题。
- 动画开关或简单强度选择。
- 显示当前主题名称。
- 显示“已应用 / 应用失败”短状态。

不允许在侧边栏配置：

- 单独颜色。
- 卡片材质细节。
- 圆角。
- 阴影。
- 透明度。
- 粒子发射器。
- 粒子颜色。
- 动画曲线。
- 字体和内容密度。

### 3.2 侧边栏推荐结构

```text
Notification Hub
────────────────────
未读数量
最近通知
最近 3 条通知
[打开通知中心]
────────────────────
Runtime
● 正常运行
Pipe 已连接 · 卡片 2 张
[查看诊断]
────────────────────
快速调整
音量        ━━━━━ 80%
[静音]
主题        Mint Night ▾
动画        开启
```

首屏优先级：

```text
通知 > Runtime 健康 > 快速声音 / 视觉调整
```

开发测试功能不得长期占据侧边栏首屏：

- Shelf 参数。
- 创建测试卡片。
- 清空测试卡片。
- SceneState JSON。
- Runtime 详细日志。

这些内容应迁移到明确的产品区域，而不是继续堆在侧边栏：

| 当前侧边栏内容 | 最终归属 | 说明 |
|---|---|---|
| Shelf 停靠位置、方向、间距 | Runtime / 桌面布局设置 | 用户可配置的产品能力 |
| 当前 SceneState 摘要 | Runtime / 当前场景 | 用用户能理解的摘要展示 |
| SceneState 原始 JSON | 诊断中心 | 面向排障和开发，不放普通首屏 |
| Runtime 详细状态、错误和生命周期 | 诊断中心 | 展示错误码、traceId、重连和恢复证据 |
| 创建测试卡片、清空测试卡片 | 开发与验收工具 | 明确标记为开发工具，不写入通知历史 |
| 测试布局、测试关闭交互 | 开发与验收工具 | 保留 Runtime 回归验收能力 |

第一阶段不立即拆出大量新页面。可以先使用现有设置页承载：

```text
设置页
└─ Runtime
   ├─ 桌面布局
   ├─ 当前场景摘要
   └─ 高级诊断 / 开发与验收入口
```

等 Runtime 生命周期和布局能力收口后，再根据真实使用频率拆分独立的 Runtime、诊断和开发工具 Surface。

---

## 4. 页面负责的内容

页面是完整配置和深度管理中心。

页面负责：

### Notification Center

- 完整通知历史。
- 搜索和筛选。
- 排序。
- 详情。
- 批量操作。
- 删除和清空。
- 通知状态。
- 诊断链。
- 来源会话。
- 重新显示。
- 视觉测试入口。

### Runtime / 诊断页面

- Runtime 详细状态。
- Named Pipe 状态。
- 进程生命周期。
- 错误码。
- traceId。
- 重连记录。
- SceneState。
- 卡片布局详情。
- 诊断事件链。
- 故障注入。
- 诊断导出。

### 声音页面

- 全局声音开关。
- 默认音量。
- 按通知类型配置。
- 按重要性配置。
- 重复通知策略。
- 工作模式静音规则。
- 声音文件检查。
- 声音预览。
- 播放失败回退。
- 保存、应用和失败状态。

### 视觉与主题页面

- 主题列表。
- 主题预览。
- 卡片材质。
- 颜色 Token。
- 圆角。
- 阴影。
- 动画强度。
- 粒子模式。
- 透明度。
- 内容密度。
- 字体和 fallback。
- 可读性检查。
- 真实通知预览。

### 模式页面

- 新建模式。
- 编辑模式。
- 模式继承。
- 字段覆盖。
- 最终值解释。
- 预览。
- 保存。
- 应用。
- 失败回退。
- 恢复默认。

### 公共 API 页面

- API 版本。
- 能力发现。
- 插件身份。
- 权限范围。
- 调用记录。
- 错误诊断。
- 版本迁移。
- 示例调用。

---

## 5. 侧边栏宽度与响应式原则

侧边栏宽度必须先通过真实 Hana 测量，不能只根据 CSS 猜测。

需要区分：

```text
Widget 页面自身宽度
Hana Surface 宿主宽度
实际可用内容宽度
```

初步目标：

```text
最小可用宽度：约 360px
舒适宽度：约 400～460px
```

最终数值以真实 Hana 容器测量结果为准。

可读性目标：

- 通知标题最多显示两行。
- 通知摘要显示两到三行。
- 重要元数据不被过早截断。
- Runtime 状态摘要完整可读。
- 音量滑块有足够操作区域。
- 主题名称不会在正常宽度下被截断。
- 中文和 emoji 不出现异常断行。
- 次要字段可以省略，但核心状态不能省略。

布局原则：

- 间距采用 4px 倍数。
- 重要区域之间使用足够留白。
- 细线和层级代替粗边框。
- 正文对比度达到可读标准。
- 所有按钮、输入框和滑块具备默认、悬停、激活、禁用和焦点状态。
- 窄屏时优先减少次要文本，不压缩所有核心控件。
- 自适应网格优先于固定双列。
- 长标题、来源和摘要允许合理换行。

### 5.1 自适应是硬性要求

侧边栏和页面必须采用真正的自适应布局，而不是只设置一个较大的固定宽度。

必须满足：

```text
宽屏：充分利用可用空间，保持舒适留白
中等宽度：自动调整网格和按钮布局
窄屏：双列内容重排为单列，长文本换行
最小宽度：不遮挡、不裁切、不出现横向滚动条
```

截图已经确认：当前 Widget 缩小到最小时出现横向滚动条和右侧内容遮挡。因此这属于阶段 0 的已知回归问题，后续必须先建立复现测试，再修改布局。

自适应验收至少覆盖以下宽度档位：

```text
宽屏：≥ 480px
中等：360px～479px
窄屏：300px～359px
最小：宿主实际允许的最小宽度
```

每个宽度档位都必须检查：

- `document.documentElement.scrollWidth <= document.documentElement.clientWidth`。
- `document.body.scrollWidth <= document.body.clientWidth`。
- 不出现横向滚动条。
- 通知标题和摘要没有被容器裁切。
- Runtime 状态字段可以换行或降级显示。
- 按钮文字完整可见，必要时改为单列堆叠。
- 未来音量滑块不会溢出容器。
- 未来主题选择控件不会挤出容器。

实现优先采用：

- `minmax(0, 1fr)`。
- `auto-fit` / `auto-fill`。
- `flex-wrap`。
- `min-width: 0`。
- `overflow-wrap: anywhere`。
- 容器查询或等效的组件级响应式策略。
- 窄屏下的单列布局。

禁止用以下方式掩盖问题：

- `overflow-x: hidden` 直接隐藏溢出。
- 继续缩小字体、按钮和间距直到内容不可读。
- 用省略号隐藏通知标题、Runtime 状态或主题名称等核心信息。
- 只测试当前 594px 宽度，不测试最小宽度。

`overflow-x: hidden` 只有在已经证明不存在布局溢出、且用于防止浏览器亚像素误差时才可以使用；不能作为修复方案。

禁止：

- 只通过增加 `max-width` 宣称宽度问题已解决。
- 用大量省略号隐藏核心状态。
- 为了塞入更多功能而持续压缩字体和间距。
- 在侧边栏复制完整设置页面。

---

## 6. 状态同步原则

侧边栏和页面必须共享同一领域状态，不能各自维护一套设置。

数据关系：

```text
SettingsStore
  ├─ Sidebar View Model
  └─ Settings Page View Model
```

正确行为：

```text
页面修改音量为 65%
→ SettingsStore 更新
→ Runtime 应用
→ 侧边栏显示 65%

侧边栏修改音量为 80%
→ SettingsStore 更新
→ Runtime 应用
→ 页面显示 80%
```

不允许出现：

```text
侧边栏显示 80%
页面显示 65%
Runtime 实际是 65%
```

每次快捷设置都必须区分：

```text
正在应用
已保存
已应用
应用失败
已回退
```

侧边栏显示简短状态，页面显示完整解释和诊断。

主题切换和声音调整都必须支持：

- 当前值读取。
- 单一数据源更新。
- Runtime 应用。
- 成功反馈。
- 失败回退。
- 页面与侧边栏同步。

---

## 7. 后续开发前的强制检查清单

在修改侧边栏、Widget 或页面前，必须先读取本文档，并回答：

1. 这项功能属于侧边栏快速工具，还是页面详细配置？
2. 是否会让侧边栏承载复杂配置？如果会，应移到页面。
3. 是否使用统一的领域状态，而不是在 UI 内维护副本？
4. 是否有保存、应用、失败和回退状态？
5. 是否会增加侧边栏首屏密度？
6. 是否需要真实 Hana 宽度和视觉验收？
7. 是否会影响 alpha.8 已验证的 Runtime 卡片交互？
8. 是否先写了回归测试？
9. 是否保留了旧版 `legacy-reference` 隔离？
10. 是否避免提前引入模式制作或公共 API？

如果功能属于以下类别，默认放页面：

```text
复杂搜索和筛选
完整通知历史
批量管理
详细 Runtime 诊断
声音规则
声音文件管理
视觉参数编辑
主题制作
模式制作
公共 API 管理
```

如果功能属于以下类别，可以放侧边栏：

```text
未读数量
最近通知
通知摘要
Runtime 健康摘要
简单音量
静音
快速切换已注册主题
简单动画开关
```

---

## 8. 最终产品边界

### 侧边栏

```text
通知简要
Runtime 状态
简单音量控制
快速视觉控制
```

### 页面

```text
完整通知管理
Runtime 详细诊断
声音完整设置
视觉主题编辑
模式制作
公共 API 管理
```

最终原则：

> 侧边栏是轻量化的快速工具，负责感知当前状态和完成高频小调整；页面负责完整配置、预览、解释、诊断和高级管理。
