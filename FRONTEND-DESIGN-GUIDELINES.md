# Notification Hub vNext
# 前端设计指导文件

> 文档用途：这是 Notification Hub vNext 侧边栏、Widget、Notification Center、设置页面、Runtime 页面和诊断页面的前端设计基准。
>
> 上下文压缩或开启新一轮实现后，开始任何前端设计、页面结构、UI 组件、样式或路由工作前，必须先读取本文档和 `SIDEBAR-PAGE-PRODUCT-PLAN.md`。
>
> 本文档记录 2026-08-11 已确认的前端方向。若要改变其中的核心结构，必须先重新讨论并更新本文档，不得在实现中悄悄偏离。

---

## 1. 设计总原则

Notification Hub vNext 的前端应当像一个完整的应用，而不是一张堆满表单的网页或一排僵硬的文字命令。

目标感受：

```text
安静
精确
有技术感
信息清楚
操作自然
视觉克制
```

核心原则：

1. 顶部负责进入大类空间。
2. 进入大类后，每个页面独立完成自己的职责。
3. 设置按领域拆成独立页面，不做一张无限增长的长表单。
4. 通知历史完整保留；Notification Center 页面显示上限由用户设置，支持 30、100、500、1000、无限和自定义。
5. 单条通知默认只显示短摘要，完整内容通过“查看详情”展开。
6. 侧边栏只做轻量快速工具，页面负责详细配置。
7. 宽屏利用空间，窄屏自动重排，任何宽度不遮挡核心内容。
8. 当前先做结构和可用性，最终主题和整体美化留到声音、视觉、模式能力成熟后集中完成。

一句话：

> 顶部导航负责“去哪里”，独立页面负责“把一件事做好”，通知卡片负责“先给摘要，需要时再展开”。

---

## 2. 产品层级与页面层级

整体结构：

```text
Notification Hub
├─ 侧边栏 / Widget：轻量快速工具
│
├─ 顶部大类导航：进入主要空间
│
├─ 通知中心：通知历史和通知管理
├─ 设置中心：设置领域入口
├─ Runtime：桌面运行状态和场景
└─ 诊断中心：错误、恢复和开发验收
```

### 2.1 顶部大类导航

顶部导航不能只是：

```text
通知中心 | 设置 | Runtime | 诊断
```

这种形式过于僵硬，像命令行或后台管理系统。

顶部导航应采用图像化导航卡片：

```text
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│      ◉        │  │      ⚙        │  │      ◈        │
│   通知中心     │  │      设置      │  │    Runtime    │
│  最近通知入口   │  │  声音与视觉设置  │  │  桌面运行状态   │
└───────────────┘  └───────────────┘  └───────────────┘
```

每张导航卡片必须包含：

- 统一风格的线性 SVG 图标。
- 页面名称。
- 一句简短说明。
- 默认态。
- hover 态。
- active 态。
- focus-visible 态。
- 可访问的 `aria-label`。
- 可选的状态摘要，例如未读数量或 Runtime 异常。

不得只使用 emoji 作为图标。图标要使用内嵌 SVG，保证 Windows Hana 环境中的视觉一致性。

默认页面：

```text
通知中心
```

### 2.2 顶部导航的响应式行为

宽屏：

```text
四张导航卡片横向排列
```

中等宽度：

```text
两列两行
```

窄屏：

```text
两列紧凑卡片
```

极窄：

```text
单列排列
```

禁止：

```css
white-space: nowrap;
overflow-x: hidden;
```

不得通过隐藏横向溢出来掩盖导航布局错误。

推荐方向：

```css
.page-nav {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 12px;
}

.page-nav-card {
  min-width: 0;
}
```

最终断点必须通过真实 Hana 容器测量和回归测试确认，不能只凭一个固定 viewport 数值。

---

## 3. 多页面结构

用户体验上采用多页面结构，但不一开始注册大量 Hana 顶层入口。

推荐采用：

```text
少量顶层 Surface
→ 页面内部路由或独立领域页面
→ 每个页面只负责一个清晰领域
```

核心页面：

```text
/notification-center
/settings
/runtime
/diagnostics
```

设置领域页面可以逐步增加：

```text
/settings/sound
/settings/layout
/settings/visual
/settings/history
```

是否将这些路由全部注册为 Hana 顶层 Page Surface，应根据 Hana 的页面承载能力和实际导航体验渐进决定。视觉上必须保持多页面体验，运行时不要产生十几个混乱的顶层入口。

### 3.1 通知中心

通知中心负责：

- 全部通知。
- 未读通知。
- 重要通知。
- 错误通知。
- 聊天通知。
- 频道通知。
- 工具通知。
- 系统通知。
- 搜索。
- 排序。
- 通知详情。
- 批量标记已读。
- 删除和清理。

### 3.2 设置中心

设置中心首先是设置领域入口，不打开后直接堆出几十项表单。

设置首页采用领域卡片：

```text
设置中心

┌──────────────┐  ┌──────────────┐
│    音频图标    │  │    布局图标    │
│   声音设置     │  │   桌面布局     │
│ 音量与通知声音  │  │ Shelf 与卡片位置│
└──────────────┘  └──────────────┘

┌──────────────┐  ┌──────────────┐
│    视觉图标    │  │    历史图标    │
│  视觉与主题    │  │  历史与隐私    │
│ 主题、动画预览  │  │ 保存与清理策略  │
└──────────────┘  └──────────────┘
```

点击后进入独立领域页面：

```text
设置中心 → 声音设置
设置中心 → 桌面布局
设置中心 → 视觉与主题
设置中心 → 历史与隐私
```

不建议把粒度拆到每一个小控件：

```text
不推荐：音量一页、静音一页、默认策略一页
推荐：声音设置一页，内部包含相关完整设置
```

一个独立页面对应一个完整设置领域。

### 3.3 Runtime 页面

Runtime 页面面向用户，负责：

- Runtime 当前状态。
- 桌面卡片摘要。
- 当前场景摘要。
- 当前布局摘要。
- 恢复与持久化状态。
- 用户可理解的 Runtime 操作。

Runtime 页面不承担原始日志和大量技术 JSON。

### 3.4 诊断中心

诊断中心面向排障和开发，负责：

- Runtime 详细状态。
- Named Pipe 状态。
- 进程生命周期。
- 重连记录。
- 错误记录。
- 错误码和 traceId。
- SceneState 原始数据。
- 布局同步状态。
- 日志导出。
- 开发与验收工具。

### 3.5 开发与验收工具

以下能力移出侧边栏首屏，迁移到诊断中心的开发与验收区域：

- 创建测试卡片。
- 清空测试卡片。
- 测试 Shelf。
- 测试关闭交互。
- 测试布局重排。

必须明确标记：

```text
开发工具
仅用于 Runtime 验收
不会写入通知历史
```

---

## 4. 通知中心设计

### 4.1 通知数量不限制

通知中心必须保留完整通知历史，但页面不能默认一次性渲染数千张卡片：

```text
历史数据：全部保留
页面显示上限：默认 100，可选 30 / 100 / 500 / 1000 / 无限 / 自定义
单条卡片：默认压缩高度
完整内容：查看详情时展示
```

显示上限只控制当前页面的查询和渲染负载，不删除 Store 记录，不清理历史文件。`无限`表示取消路由级上限，数量极大时仍可能增加页面渲染成本。

不得把需求误解成“默认只显示最近 5 条”。

数据完整性和视觉密度必须分开：

```text
数据不截断
视觉摘要截断
详情不截断
```

### 4.2 页面布局

```text
┌────────────────────────────────────┐
│ [图标] 通知中心       Runtime ● 正常 │
│ 查看最近通知，按需要筛选和处理        │
├────────────────────────────────────┤
│ [全部] [未读] [重要] [聊天]           │
│ [频道] [工具] [系统] [错误]           │
├────────────────────────────────────┤
│ 已选 0 条                  [刷新]     │
├────────────────────────────────────┤
│ 通知卡片                            │
│ 通知卡片                            │
│ 通知卡片                            │
└────────────────────────────────────┘
```

默认通知列表可以完整呈现所有通知，但通过单条卡片压缩和必要的分页、虚拟化或滚动保持页面可用。当前阶段优先完成单条卡片压缩，不擅自隐藏通知记录。

### 4.3 单条通知卡片

默认卡片应接近：

```text
┌──────────────────────────────────┐
│ ● 工具执行失败             20:31 │
│ 工具调用返回了错误信息，具体原因… │
│ hana.tool · 未读        [查看详情] │
└──────────────────────────────────┘
```

默认展示：

- 重要性标识。
- 时间。
- 一行标题。
- 两行以内摘要。
- 来源。
- 状态。
- “查看详情”主要操作。

默认不展开：

- 完整正文。
- 完整 metadata。
- 过多技术字段。
- 多个完整文字按钮。

推荐 CSS 行数限制：

```css
.notification-card h2 {
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.notification-content {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  overflow-wrap: anywhere;
}
```

完整标题和正文必须保留在数据中，并在详情中完整显示。

### 4.4 查看详情

单条通知只通过“查看详情”打开完整内容。

详情面板建议位于当前列表之前或当前卡片展开位置，避免窄页面出现难以操作的右侧抽屉。

```text
┌──────────────────────────────────┐
│ 工具执行失败             [关闭详情] │
├──────────────────────────────────┤
│ 完整正文                          │
│ 来源：hana.tool                   │
│ 类型：tool_error                  │
│ 状态：formatted                   │
│ 时间：2026-08-11 20:31            │
│ 元数据                            │
│ {...}                             │
│ [标记已读]                        │
└──────────────────────────────────┘
```

查看详情默认不自动改变通知已读状态，保留已有语义。

### 4.5 通知卡片宽度与密度

宽页面：

```text
卡片最大宽度：760～880px
卡片内边距：16～20px
卡片间距：10～12px
```

窄页面：

```text
卡片内边距：12～14px
来源、类型、状态允许换行
操作区必要时单独占一行
```

不让来源和类型比通知标题更抢眼。

---

## 5. 设置领域页面设计

### 5.1 声音设置

```text
← 返回设置中心

声音设置
调整通知声音的播放规则和音量

声音状态
● 已应用

基础设置
├─ 全局声音
├─ 默认音量
└─ 工作模式静音

高级策略
├─ 按通知类型
├─ 按重要性
└─ 重复通知抑制

预览
[播放示例声音]

[保存并应用]
```

### 5.2 桌面布局

```text
← 返回设置中心

桌面布局
调整通知卡片的排列和显示位置

当前布局
Shelf · 右下 · 间距 12px

布局设置
├─ 停靠位置
├─ 排列方向
├─ 卡片间距
└─ 显示器

当前场景
当前卡片：2 张
最后同步：刚刚

[保存并应用]
```

### 5.3 通知视觉（卡片工作室）

通知视觉页是卡片工作室，不是工程工作台。一次只做一个决定：

```text
← 返回设置中心

通知视觉                              [保存]
选一种出现方式，只调这一组，看见它怎么动，再保存。

堆叠 | 弹幕 | 突脸（未实现）

[ 只属于当前出现方式的一组旋钮 ]
[ 卡片外观：尺寸 / 圆角 / 背景 ]

预览（只演当前这一种）     [试一条]
```

硬约束：

1. 出现方式只留一个入口。无关旋钮是「不出现」，不是折叠。
2. 预览只演当前这一种行为。禁止三张通道卡并排。
3. 全页只有一个实心主按钮「保存」。试一条贴在预览旁，走真实出卡。
4. 配置包、应用到事件、诊断默认折叠。
5. 看得见的都能用。`overflow` 不露。悬停暂停在弹幕「这张卡」露出，与悬停加亮独立。突脸可见但不可选，带文字角标「未实现」。

规格：`docs/superpowers/specs/2026-09-12-visual-studio-elegance.md`。

### 5.4 设置项统一结构

每个设置项必须包含：

```text
设置名称
简短说明
控件
当前状态
```

每次保存必须区分：

```text
正在应用
已保存
已应用
应用失败
已回退
```

---

## 6. 侧边栏设计

侧边栏是轻量化的快速工具，不是完整设置中心。

侧边栏只负责：

1. 通知简要。
2. Runtime 状态摘要。
3. 简单音量控制。
4. 快速视觉设置。

侧边栏不负责：

- 完整通知历史。
- 复杂搜索和筛选。
- 批量操作。
- 详细 Runtime 诊断。
- SceneState 原始 JSON。
- Shelf 完整参数表单。
- 创建测试卡片。
- 完整声音规则。
- 视觉主题编辑。
- 模式制作。
- 公共 API 管理。

推荐结构：

```text
Notification Hub
├─ 最近通知
│  ├─ 未读数量
│  ├─ 最近几条摘要
│  └─ 打开通知中心
│
├─ Runtime
│  ├─ 正常 / 连接中 / 异常
│  ├─ 当前卡片数量
│  └─ 查看诊断
│
└─ 快速调整
   ├─ 静音
   ├─ 音量
   ├─ 快速主题
   └─ 动画开关
```

侧边栏中的通知同样采用短卡片摘要，点击后进入通知中心详情。

---

## 7. 响应式布局硬性要求

侧边栏和页面必须真正自适应。

```text
宽屏：充分利用可用空间，保持舒适留白
中等宽度：自动调整网格和按钮布局
窄屏：双列重排为单列，长文本换行
最小宽度：不遮挡、不裁切、不出现横向滚动条
```

建议验收宽度：

```text
宽屏：≥ 480px
中等：360px～479px
窄屏：300px～359px
最小：宿主实际允许的最小宽度
```

每个宽度必须检查：

```js
document.documentElement.scrollWidth <= document.documentElement.clientWidth
document.body.scrollWidth <= document.body.clientWidth
```

并检查：

- 无横向滚动条。
- 通知标题和摘要没有被容器裁切。
- Runtime 状态可以换行或降级显示。
- 按钮文字完整可见，必要时改为单列。
- 音量滑块不溢出。
- 主题选择控件不溢出。
- 导航卡片不重叠。

优先使用：

```css
minmax(0, 1fr)
auto-fit / auto-fill
flex-wrap
min-width: 0
overflow-wrap: anywhere
单列重排
容器查询或等效组件级响应式策略
```

禁止：

```css
overflow-x: hidden;
```

直接隐藏布局溢出。只有在已经证明不存在布局错误、且仅用于消除浏览器亚像素误差时才允许使用。

---

## 8. 基础视觉方向

当前阶段采用：

```text
Swiss 极简骨架
+
深色技术基底
+
Mint 绿色交互色
+
克制的 Bento 分组
```

不要做：

- 大面积渐变。
- 过度毛玻璃。
- 霓虹发光。
- 大量装饰性圆角卡片。
- 每个状态都使用高饱和色。
- 为了视觉效果牺牲可读性。

### 8.1 基础颜色 Token

默认主题可沿用 Mint Night：

```css
--bg: #0f1614;
--surface: #17221f;
--surface-raised: #1d2b27;
--surface-soft: #14201d;

--text: #e7f2ee;
--text-muted: #9bb1a9;
--line: #304740;

--accent: #62d0a8;
--accent-strong: #38b88d;
--accent-ink: #092118;

--success: #72d49e;
--warning: #f3c66d;
--danger: #f18c8c;
```

颜色职责：

```text
Mint：交互、链接、选中和正常强调
绿色：已连接、已应用、成功
黄色：等待、已保存但未应用、注意
红色：错误、断开、应用失败
灰色：次要文字、边框、辅助信息
```

颜色不能用来装饰普通文本。

### 8.2 尺寸 Token

```text
4px   图标与文字之间
8px   控件内部间距
12px  紧凑列表间距
16px  卡片内边距
24px  区域间距
32px  页面主要留白
48px  页面上下留白
```

圆角：

```text
按钮：6px
输入框：6px
普通面板：8px
详情面板：8px
状态徽标：999px
```

字体：

```css
font-family: "Segoe UI", "Microsoft YaHei", sans-serif;
```

诊断数字和原始数据：

```css
font-family: Consolas, "Cascadia Code", monospace;
```

字号：

```text
页面标题：26～28px
区域标题：18～20px
卡片标题：15～16px
正文：14px
侧边栏正文：13px
辅助信息：12px
```

辅助文字不得小于 11px。

---

## 9. 页面状态与交互

每个交互控件必须有：

```text
默认态
hover 态
active / pressed 态
disabled 态
focus-visible 态
```

页面必须明确处理：

- 加载中。
- 空状态。
- API 错误。
- 应用中。
- 已保存。
- 已应用。
- 应用失败。
- 回退完成。

错误信息不能只显示错误码，要包含：

```text
发生了什么
用户可以做什么
是否会自动恢复
```

技术详情可以折叠，不把原始 JSON 作为默认第一信息。

---

## 10. 当前实现顺序

当前不做最终主题大美化，按以下顺序实现：

### 第一步：通知卡片压缩与显示负载控制

- 保持完整通知历史。
- 默认显示上限为 100，用户可切换 30 / 100 / 500 / 1000 / 无限 / 自定义。
- 默认限制单条标题为一行。
- 默认限制单条摘要为两行。
- 保留完整正文和 metadata。
- 查看详情显示完整信息。
- 不改变查看详情不自动标记已读的语义。

### 第二步：页面顶部图像化导航卡片

- 默认进入通知中心。
- 增加通知中心、设置、Runtime、诊断导航卡片。
- 统一 SVG 图标。
- active、hover、focus 和响应式状态。
- 导航卡片不横向溢出。

### 第三步：设置入口页和领域独立页面

- 设置中心领域卡片。
- 声音设置页预留 / 实现。
- 桌面布局设置页。
- 视觉与主题页预留。
- 历史与隐私页后续实现。

### 第四步：Runtime 与诊断页面分流

- Runtime 用户摘要页。
- 诊断详细状态页。
- SceneState 摘要和原始数据分层。
- 开发与验收工具迁移。

### 第五步：声音、视觉、模式接入

按照 `SIDEBAR-PAGE-PRODUCT-PLAN.md` 的阶段顺序接入，不提前做完整主题库。

### 第六步：最终视觉美化

等声音、视觉、模式等基础能力成熟后，再集中完成：

- 主题库扩展。
- 侧边栏整体美化。
- 页面整体美化。
- 动画细节。
- 卡片材质。
- 阴影。
- 圆角。
- 图标细节。
- 状态动效。
- 浅色、深色和高对比度主题。
- 真实 Hana 视觉回归。

---

## 11. 强制检查清单

开始任何前端实现前，必须确认：

1. 这是侧边栏快速工具，还是页面详细配置？
2. 顶部导航是否使用图像化导航卡片，而非僵硬文字菜单？
3. 这项设置是否应该属于一个完整设置领域页面？
4. 通知数量是否仍然完整保留？
5. 单条通知是否只在视觉上压缩，而不是删除正文？
6. 查看详情是否仍然可以看到完整正文和 metadata？
7. 是否会产生新的横向溢出？
8. 窄宽度下是否会自动单列重排？
9. 是否使用统一 Token，而不是页面单独发明颜色？
10. 是否区分正在应用、已保存、已应用、失败和回退？
11. 是否会影响 alpha.8 已验证的桌面 Runtime 行为？
12. 是否先写了一个描述用户行为的回归测试？
13. 是否保留旧版 `legacy-reference` 隔离？
14. 是否避免提前实现模式制作、公共 API 和跨平台 Runtime？

最终边界：

```text
侧边栏：轻量快速工具
页面：完整领域功能
导航卡片：进入大类空间
通知卡片：短摘要
详情面板：完整内容
诊断中心：技术证据
开发工具：Runtime 验收
```
