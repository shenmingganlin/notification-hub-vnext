# Notification Hub vNext 修复报告与使用方法

日期：2026-08-03

本文档用于交给 Hana，说明 `notification-hub-vnext` 的 BUG-5 修复内容、安装方式和人工验收方法。

## 重要边界

- 只操作插件 `notification-hub-vnext`。
- 旧插件 ID 是 `notification-hub`，不得修改、停止、删除、替换，也不得使用旧插件的 Runtime 或数据作为 vNext 依赖。
- 真实安装必须使用 Hana 的 ZIP 拖放界面；不能只凭 ZIP 静态检查宣称安装成功。
- 替换已运行的 vNext 前，先在 Hana 中禁用 vNext，避免 Windows 文件锁定。
- 若需要重启进程，只允许确认可执行文件路径属于 vNext 后再操作。

## 一、交付物

修复包：

```text
C:\Users\Ganlin\Desktop\OH-WorkSpace\notification-hub-upgrade\dist\notification-hub-vnext-0.1.0-alpha.1.zip
```

SHA256：

```text
3954576A85207FFEBBCD81CA1C1F54065A2B8DD4CFEACF1F1806E6868E275293
```

ZIP 必须满足：

- 根目录存在 `manifest.json`；
- `manifest.json` 的插件 ID 为 `notification-hub-vnext`；
- `runtime/notification-hub-runtime.exe` 存在；
- 不包含旧版 `notification-hub` 插件、旧 helper 或旧数据文件。

已安装 vNext 的预期 Runtime 路径：

```text
C:\Users\Ganlin\.hanako\plugins\notification-hub-vnext\runtime\notification-hub-runtime.exe
```

## 二、原问题 BUG-5

用户在 Native Runtime 窗口上拖动卡片或点击关闭后，Runtime 内部状态发生了变化，但宿主 Host 没有收到这些 Native UI 变化。

因此会出现：

1. `scene-state.json` 仍保存旧坐标和旧卡片列表；
2. Host 内存中的恢复计划仍然过期；
3. Runtime 重启后按照旧恢复计划重放；
4. 已关闭的卡片重新出现，已拖动的卡片回到旧位置。

另外，主 Scene 窗口关闭后原先没有明确的持久化表示，重启时可能复活。

## 三、修复内容

### 1. Runtime 主动推送场景变更

Runtime 新增非请求事件：

```json
{
  "type": "event",
  "payload": {
    "eventType": "scene.changed",
    "result": {
      "sceneStateSnapshot": {}
    }
  }
}
```

Runtime 在以下变化后发送最新快照：

- 卡片拖动后位置改变；
- 卡片被关闭或原生窗口销毁；
- 主 Scene 窗口被关闭或销毁。

### 2. Host 接收并持久化

- `PipeClient` 将 `event` 消息路由到事件通道；
- `RuntimeProcessManager` 收到 `scene.changed` 后更新内存恢复计划；
- 现有 SceneState 持久化协调器继续使用 debounce 机制写入 `scene-state.json`；
- Host 命令 ACK 中的局部快照不会覆盖完整恢复计划，避免部分重放状态覆盖完整状态。

### 3. 主 Scene 窗口关闭状态

SceneState 现在允许：

```json
"sceneWindow": null
```

当主 Scene 窗口被关闭时，恢复投影不会再生成 `scene.update`，所以 Runtime 重启后不会复活该窗口。

### 4. 回归测试

新增或强化了：

- SceneState `sceneWindow: null` 校验与恢复投影；
- Runtime Controller 的拖动、卡片关闭、主窗口关闭同步；
- Named Pipe 真实 Win32 卡片拖动/关闭事件测试；
- ProcessManager 对 `scene.changed` 的恢复计划和持久化测试。

## 四、自动验证证据

在仓库目录：

```text
C:\Users\Ganlin\Desktop\OH-WorkSpace\notification-hub-upgrade
```

已执行并通过：

```text
npm run check
npm test
```

Node 结果：`85 tests`，`74 passed`，`11 skipped`，`0 failed`。跳过项是没有显式传入 Runtime 路径的 Node smoke 测试。

本轮可复现的 Runtime self-test：

```text
protocol: ok
layout: ok
scene-controller: ok
hit-test: ok
work-area: ok
```

当前环境未提供 `ctest` 命令，因此 CTest 25 项仅作为既有构建记录，未在本轮重新执行。Node 测试覆盖 Runtime Host/Client 生命周期取消、恢复快照 key 校验、恢复幂等键、SceneState 持久化监听和迟到响应屏蔽。Runtime self-test 不替代 Hana 真实 UI 安装验收。

## 五、Hana 安装方法

1. 确认当前准备安装的是上面列出的 ZIP，并核对 SHA256。
2. 在 Hana 插件管理界面只禁用 `notification-hub-vnext`。
3. 不要禁用、删除或替换旧插件 `notification-hub`。
4. 将 ZIP 拖入 Hana 的真实安装区域。
5. 等待 Hana 报告 vNext `loaded` / `activated`。
6. 重新启用 `notification-hub-vnext`。
7. 确认 vNext Runtime 路径为：

   ```text
   C:\Users\Ganlin\.hanako\plugins\notification-hub-vnext\runtime\notification-hub-runtime.exe
   ```

8. 若出现 Windows `EPERM` 或文件被占用，不要强行覆盖活动目录；先禁用 vNext，再按第 2 步重新安装。

## 六、人工验收方法

请只测试 vNext Native Runtime，不把 Hana 普通 Toast 或旧插件窗口计入结果。

### A. 基础显示

创建两张内容非空的 vNext Native Scene 卡片，确保：

- 卡片真实可见，不是只有窗口句柄；
- 中文和 emoji 正常显示；
- 左上角没有持续占用、隐形、未响应的区域；
- 不出现安装后的红色错误文字。

### B. 拖动与关闭

1. 拖动第一张卡片到明显的新位置，记录实际位置。
2. 关闭第二张卡片，确认它消失。
3. 如测试场景包含主 Scene 窗口，再关闭主 Scene 窗口。
4. 等待至少 1 秒，让 debounce 持久化完成。
5. 打开 vNext `dataDir` 下的 `scene-state.json`。默认文件名是 `scene-state.json`，实际路径以 Hana 传入 vNext 的 `dataDir` 为准。
6. 检查：

   - `cardOrder` 不包含已关闭的卡片；
   - 拖动卡片的 `x/y` 是新位置；
   - 主 Scene 窗口已关闭时，`sceneWindow` 为 `null`。

### C. Runtime 重启恢复

优先使用 Hana/vNext 自身的 Runtime 重启功能。若必须手动操作，只能确认目标路径属于：

```text
C:\Users\Ganlin\.hanako\plugins\notification-hub-vnext\runtime\notification-hub-runtime.exe
```

重启后检查：

- 已关闭的卡片不再复活；
- 拖动后的卡片保持新位置；
- 已关闭的主 Scene 窗口不再复活；
- `scene-state.json` 与实际窗口集合一致；
- vNext 可以继续创建新卡片；
- 旧插件状态保持不变。

## 七、失败时请返回的报告

如果人工验收失败，请不要先重新安装旧插件或删除数据。请返回以下信息：

```text
1. Hana 显示的插件 ID、loaded/activated 状态和错误文本
2. 实际 vNext Runtime 可执行文件完整路径
3. ZIP SHA256
4. 失败发生在：安装、显示、拖动、关闭、持久化还是重启恢复
5. 失败前后的 scene-state.json 内容
6. 卡片标题、初始位置、拖动后位置
7. 是否关闭了主 Scene 窗口
8. 旧插件 notification-hub 是否仍保持原状态
9. vNext Runtime 的 stdout/stderr 或 Hana diagnostic 内容
10. 截图或录屏，尤其是窗口左上角区域和重启后的恢复结果
```

## 八、验收结论模板

```text
安装结果：通过 / 失败
vNext Runtime 路径：
ZIP SHA256：
基础显示：通过 / 失败
中文和 emoji：通过 / 失败
拖动后 SceneState 坐标同步：通过 / 失败
关闭卡片后 SceneState 删除：通过 / 失败
关闭主 Scene 窗口后 sceneWindow:null：通过 / 失败 / 未测试
Runtime 重启后位置保持：通过 / 失败
Runtime 重启后已关闭卡片不复活：通过 / 失败
Runtime 重启后主 Scene 窗口不复活：通过 / 失败 / 未测试
旧版 notification-hub：未触碰且状态正常 / 异常
失败证据：
```

