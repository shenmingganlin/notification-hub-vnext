# 通知视觉 · 飞法与运动

本机通知卡如何占槽、如何来去、如何上妆。词表只记概念，不记控件和系数。

## 语言

**飞法（flight）**：
一张卡怎么存在于桌面：堆叠或弹幕。路径是以后的新飞法、新池。
_避免使用_：behavior 当飞法、弹窗当现行飞法

**飞法通道（flightChannel）**：
一种飞法一座池。堆叠一池，弹幕一池。
_避免使用_：事件巷、用户自定义通知分类

**幕布（overlay）**：
弹幕池的渲染表面。一座弹幕池一块幕布，平动弹幕都画在上面。不是飞法，不是通道法律，也不改默认带宽。
_避免使用_：覆盖层当用户词、把幕布当成新飞法、用幕布改堆叠、把打满写成默认满屏

**规约（charter）**：
一座池一份法律。配置包不携带规约。
_避免使用_：把速度、关闭方式、特效写进规约

**槽位（slot）**：
通道里的占位。排版只认槽，不认这张卡正在入场、持续还是出场。
_避免使用_：把飞入飞出当成占位、把特效当成布局

**运动（motion）**：
画面去哪。堆叠是窗口；弹幕是幕布上的精灵。入场出场是卡片位移；持续的落点是通道法律。
_避免使用_：动画当总称、把淡入写成运动、用位图上传当移动

**入场（enter）**：
这张卡来到自己槽位的运动阶段。位移是卡片动态。
_避免使用_：把淡入写进入场下拉、路径飞法

**持续（persist）**：
槽位还在时窗口如何待在槽上。通道规约字段是 **落点（settle）**：`snap` 瞬移，`follow` 追槽。
_避免使用_：PID 当用户词、把持续当成入场

**新位（newest）**：
新卡进哪一格。`dock` 占角（新卡占停靠角，旧卡沿往哪长让位）；`next` 新位（旧卡不动，新卡开下一格）。默认占角。
_避免使用_：顶部/底部、长边、把走线蛇形或回字当成新位、order/insert

**走线（wrap）**：
堆叠槽位怎么拐。`off` 关、`parallel` 平行、`snake` 蛇形、`coil` 回字。回字从停靠沿往哪长出发，转向内里，剩框越绕越小。一边的厚度按这排最厚的卡，小卡旁边留洞。
_避免使用_：回子弯当代码名、把回字写成新飞法、用走线偷偷改新位、回字里俄罗斯方块

**出场（exit）**：
这张卡离开自己槽位的运动阶段。出场一开始就交槽。位移是卡片动态。
_避免使用_：把出场飞出走写成占着槽再走

**图层（layer）**：
入场、出场画在持续背后。正在待着的卡永远压在来去的卡上面。
_避免使用_：用透明度冒充前后、把弹幕点穿和图层混谈

**侧边飞（side fly）**：
入场出场的一种位移。一般从停靠对应的屏幕边来去；沿铺开时边由这张卡的槽位和走线算出。卡片只选做法，不手写东南西北。
_避免使用_：每张卡填写 from-left、把整列涌入当成多行唯一做法

**特效（effect）**：
画什么，挂在入场、持续、出场上。不占槽、不改通道。分 **卡片特效** 与 **粒子**。
_避免使用_：特效当第四段寿命、把特效写进飞法下拉

**卡片特效（card effect）**：
画在卡面上的特效，如淡入、淡出、高光扫过。跟方案走，挂在对应阶段上。
_避免使用_：特效折页里塞位移、把淡入当飞法

**粒子（particle）**：
特效的另一门。大方向，未立法。
_避免使用_：用粒子冒充卡片特效、本刀实现粒子

**卡面（face）**：
零件树和皮。
_避免使用_：皮肤阶段、裁切刀

**事件巷（eventLane）**：
通知路由。不是飞法池。

**内容源（contentSource）**：
标题、正文零件从哪取字。跟事件，或自定义写死。只改字，不改飞法，也不让卡一直挂着。
_避免使用_：把自定义文字当成新飞法、把内容固定写成一直显示

**悬停加亮 / 悬停暂停**：
加亮是卡片皮。暂停是这张卡弹幕动态。两颗独立，可同时开。点穿开着两颗都不能用。
_避免使用_：把暂停写成通道法律、把加亮和暂停绑死成一档

## 示例对话

开发者：新卡飞进来的时候，堆要不要给它留空？
领域专家：要。槽位立刻占上，其他卡按持续补位。飞只是这张卡的入场位移，而且走在持续背后。

开发者：蛇形第四张要从下边来，是不是得在这张卡上勾「从下」？
领域专家：不要勾方向。这张卡选「沿铺开」，边由槽位和走线算出来。一般侧边则全员从停靠那一边来。

开发者：淡入放进入场下拉吗？
领域专家：不。入场下拉只谈窗口怎么来。淡入是卡片特效，挂在入场阶段上，可以和侧边飞叠。

开发者：持续的高光扫过是通道法律吗？
领域专家：不是。落点才是通道的。扫光跟这张卡的方案走。

开发者：蛇形的时候新卡怎么不占角了？
领域专家：那是旧漏洞，走线偷偷改了新位。新位是自己那档法律，跟蛇形无关。

开发者：回字和新位会不会打架？
领域专家：不打。走线是绕法，新位是新卡站路径哪一头。占角站外角，新位往心里走。

开发者：关掉开新列，回字还能点吗？
领域专家：不能。关着只剩一条边，走线三颗全灰。

开发者：弹幕打满是不是默认把带子拉满？
领域专家：不是。打满是换幕布：一张表面扛密度。带子拉满是通道法律，用户自己拉。

开发者：标题写死一句，卡是不是就一直挂在屏上？
领域专家：不是。那只是零件的内容源。事件来了仍然飞过，只是字不跟事件变。

开发者：悬停加亮和悬停暂停能一起开吗？
领域专家：能。两颗互不抢线。点穿开着两颗都灰掉。
