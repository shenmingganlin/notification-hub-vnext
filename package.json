{
  "name": "notification-hub-vnext",
  "version": "0.1.0-alpha.16",
  "private": true,
  "description": "Notification Hub vNext: a Windows notification scene system for HanaAgent.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test tests/node/*.test.mjs",
    "pressure": "node scripts/run-sound-pressure.mjs"
  }
}
